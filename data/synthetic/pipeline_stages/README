### Proof-of-Concept Datasets ###

     *Figure 7b*

 - Stages of the synthetic pipeline comparison

Datasets:
 - rdkit*: Modified RDKit images (RDKit')
 - rdkit*-aug: Augmented RDKit images (RDKit'-aug)
 - rdkit*-aug-bkg: Augmented RDKit images with background addition (RDKit-aug-bkg)
 - rdkit*-aug-bkg-deg: Augmented RDKit images with background addition, degraded (RDKit-aug-bkg-deg)

Scripts:
 - build.py:  Generates datasets from SMILES dataset

Requirements:
 - RDKit: $ conda install rdkit -c rdkit
 - svglib: $ pip install svglib
 - scikit-learn: $ conda install scikit-learn
